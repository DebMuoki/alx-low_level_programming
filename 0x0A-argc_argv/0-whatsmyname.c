#include "main.h"
#include <stdio.h>

/**
  * main - entry point
  * @argc: Number of arguments passed y program
  * @argv: array of strings containing the arguments
  * Return: 0 on success
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
